# ═══════════════════════════════════════════════════════
# Angel Guardian Technologies — L7 / HSA-001
# Unified requirements (eliminuje konflikty OpenAI/Gemini)
# ═══════════════════════════════════════════════════════

# ── AI Backend (UNIFIED: Gemini only) ────────────────────
google-genai>=1.0.0

# ── USUNIĘTO: openai (zastąpiony przez google-genai) ─────
# openai  ← USUNIĘTO

# ── USUNIĘTO: pyperclip (brak obsługi headless CI) ───────
# pyperclip  ← USUNIĘTO

# ── Monitoring (opcjonalny — brak = system działa) ───────
agentops>=0.3.0

# ── Web framework (HF Space) ─────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# ── Database ──────────────────────────────────────────────
sqlalchemy>=2.0.0
alembic>=1.13.0
asyncpg>=0.29.0          # PostgreSQL async (Supabase)
aiosqlite>=0.20.0        # SQLite fallback (dev)

# ── Utilities ────────────────────────────────────────────
python-dotenv>=1.0.0
pydantic>=2.0.0
httpx>=0.27.0

# ── Dev / CI ──────────────────────────────────────────────
pytest>=8.0.0
pytest-asyncio>=0.23.0
